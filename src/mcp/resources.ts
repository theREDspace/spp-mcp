// MCP Resources stub — full BO catalog/schema/semantic-patterns implementation
// lands in PR4. This file exists so transport.ts compiles in the interim.

import type { McpServer } from '@modelcontextprotocol/server';

export function registerBoResources(_server: McpServer): void {
  // Intentionally empty for now; populated in the next PR.
}
